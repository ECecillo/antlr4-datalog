version: "3.9"
services:
  datalog_project_run:
    build: .
    volumes:
      - .:/app
    entrypoint: python main.py

  datalog_project_terminal:
    build: .
    volumes:
      - .:/app
    entrypoint: /bin/sh
    stdin_open: true
    tty: true
